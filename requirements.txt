# Production dependencies (match pyproject.toml)
# These are managed by pyproject.toml - no need to duplicate here
# Uncomment if you need pip-only installation:
# rich>=13.7.0
# typer>=0.12.0
# pydantic>=2.6.0
# sqlglot>=23.0.0

# Development dependencies
# Install with: pip install -r requirements.txt
pytest>=8.0.0
pytest-cov>=4.1.0
mypy>=1.8.0
coverage>=7.0
