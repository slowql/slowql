[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "slowql"
version = "1.0.4"
description = "Cinematic static SQL analyzer with cyberpunk CLI"
readme = "README.md"
requires-python = ">=3.8.1"
authors = [{ name = "El Mehdi Makroumi", email = "elmehdi.makroumi@gmail.com" }]
license = { text = "Apache-2.0" }
keywords = ["sql", "analysis", "cli", "performance", "database", "cyberpunk"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Operating System :: OS Independent",
]

dependencies = [
  "pandas>=2.0",
  "rich>=13.0.0",
  "sqlparse>=0.4.0"
]

[project.optional-dependencies]
dev = [
  "pytest>=7.0.0",
  "pytest-cov>=4.0.0",
  "black>=23.0.0",
  "mypy>=1.0.0",
  "flake8>=6.0.0,<6.1.0; python_version >= '3.8.1'",
  "isort>=5.0.0"
]
docs = [
  "sphinx>=5.0.0",
  "sphinx-rtd-theme>=1.0.0"
]

[project.scripts]
slowql = "slowql.cli:main"

[tool.setuptools]
packages = ["slowql"]
package-dir = { "" = "src" }

[tool.setuptools.package-data]
slowql = ["**/*.py"]

[project.urls]
Homepage = "https://github.com/makroumi/slowql"
Repository = "https://github.com/makroumi/slowql"
Issues = "https://github.com/makroumi/slowql/issues"
